# 数据库驱动：
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 数据源名称
    name: defaultDataSource
    # 数据库连接地址
    url: jdbc:mysql://localhost:3306/nutz_test?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    # 数据库用户名&密码：
    username: root
    password: root


mybatis:
  mapper-locations:
    classpath:mapper/*.xml