spring:
  application:
    name: wk-cola-scaffold

  redis:
    host: ${redis.host}
    password: ${redis.password}
    port: ${redis.port}

  cloud:
    nacos:
      server-addr: ${NACOS_CONFIG_SERVER:ddd-nacos.wakedata-inc.com:80}
      username: nacos
      password: TkqwzJaAVP5nOvF2

      discovery:
        namespace: ${NACOS_CONFIG_NAMESPACE:ddd-dev}

      config:
        namespace: ${NACOS_CONFIG_NAMESPACE:ddd-dev}
        #        group: DEFAULT_GROUP
        file-extension: yml
        shared-configs:
            - data-id: common.yml
  #          - data-id: common-redis.yml
            - data-id: common-mq.yml
#            - data-id: common-mq-topic.yml
  #          - data-id: common-com.wake.generator.application.handler.domain-event.yml
            - data-id: common-mysql.yml
            - data-id: wk-cola-scaffold.yml

# 开启sql日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#  config-location: classpath*:/mybatis/**/*.xml

logging:
  config: classpath:logback-spring.xml

mq:
  store:
    topic: scaffold




