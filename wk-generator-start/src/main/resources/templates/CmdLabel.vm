#parse("ClassFunction.vm")
## 聚合和action
#set($AggregationName = ${classLabel.parentAggregation.name})
#set($ActionName = ${classLabel.actionName})
#set($ActionUpperName = "${ActionName.substring(0, 1).toUpperCase()}${ActionName.substring(1)}")
## 指令元素提供的数据
#set($CmdName = "${AggregationName}${ActionUpperName}Cmd")
#set($CmdPackagePath = "${globalLabel.group}.domain.${globalLabel.filed}.${classLabel.parentAggregation.name}.${CmdName}")
#set($CmdImportPackages = ${classLabel.importClassSet})
#set($CmdDescription = ${classLabel.description})
#set($CmdFields = ${classLabel.fieldLabelList})
#set($CmdMethods = ${classLabel.methodLabelList})