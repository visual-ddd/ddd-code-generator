<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>${globalLabel.group}</groupId>
    <artifactId>${globalLabel.projectName}</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>${globalLabel.projectName}-adapter</artifactId>
  <packaging>jar</packaging>
  <name>${globalLabel.projectName}-adapter</name>

  <dependencies>

    <dependency>
      <groupId>${globalLabel.group}</groupId>
      <artifactId>${globalLabel.projectName}-app</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <configuration>
            <skip>true</skip>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    </build>
</project>
