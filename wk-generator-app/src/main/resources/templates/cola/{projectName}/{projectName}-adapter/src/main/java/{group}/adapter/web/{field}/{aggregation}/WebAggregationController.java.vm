#parse("templates/Aggregation.vm")
package ${ProjectPackage}.adapter.web.${DomainName}.${AggregationLowName};

import javax.annotation.Resource;
import com.wakedata.common.core.dto.ResultDTO;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

#comment($AggregationDescription)
@RestController
@RequestMapping("/web/${AggregationLowerUnderscoreName}")
public class Web${AggregationName}Controller {

    #foreach ($cmd in $cmdList)
    @Resource
    ${cmd.name}Handler ${cmd.name.substring(0, 1).toLowerCase()}${cmd.name.substring(1)}Handler;
    #end

    #foreach ($cmd in $cmdList)
    @RequestMapping("/${cmd.actionName}")
    public ResultDTO ${cmd.actionName}(${cmd.name} cmd) {
        ${cmd.name.substring(0, 1).toLowerCase()}${cmd.name.substring(1)}Handler.handle(cmd);
        return ResultDTO.success();
    }

    #end
}
