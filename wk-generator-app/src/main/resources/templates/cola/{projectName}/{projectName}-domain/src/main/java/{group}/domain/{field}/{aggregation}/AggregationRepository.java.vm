#parse("templates/Aggregation.vm")
package $AggregationPackagePath;

#comment("${AggregationName}聚合存储接口")
public interface ${AggregationName}Repository {

    /**
     * 新增
     *
     * @param aggregation 聚合
     * @return 主键id
     */
    Long insert(${AggregationName} aggregation);

    /**
     * 更新
     *
     * @param aggregation 聚合
     */
    void update(${AggregationName} aggregation);

    /**
     * 删除
     *
     * @param id 主键
     */
    void deleteById(Long id);

    /**
     * 查询
     *
     * @param id 主键
     * @return 查询结果
     */
    ${AggregationName} selectById(Long id);

}
