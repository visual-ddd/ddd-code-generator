#parse("templates/Aggregation.vm")
package $AGGREGATION_PACKAGE_PATH;

import lombok.Data;
import lombok.EqualsAndHashCode;
import java.util.List;
import ${PROJECT_PACKAGE}.domain.DomainEntity;

#comment("${AGGREGATION_CLASS_DESCRIPTION}-聚合根")
@Data
@EqualsAndHashCode(callSuper = true)
public abstract class Abstract${AGGREGATION_CLASS_NAME} extends DomainEntity {
    #filed($AGGREGATION_CLASS_FIELDS)
    #abstractMethod(${AGGREGATION_CLASS_METHODS})

}