#parse("templates/ClassFunction.vm")
package ${IMPORT_PACKAGE_MAP.get(${CMD_ID}).get("${CMD_CLASS_NAME}")};

import lombok.AllArgsConstructor;
import lombok.Data;
import java.util.*;
import java.math.*;
import lombok.NoArgsConstructor;
import ${IMPORT_PACKAGE_MAP.get(${AGGREGATION_ID}).get("${AGGREGATION_CLASS_NAME}")}.*;

#comment("${CMD_CLASS_TITLE}-指令")
@Data
@AllArgsConstructor
@NoArgsConstructor
public class ${CMD_CLASS_NAME} {
#filed($CMD_CLASS_FIELDS)

}