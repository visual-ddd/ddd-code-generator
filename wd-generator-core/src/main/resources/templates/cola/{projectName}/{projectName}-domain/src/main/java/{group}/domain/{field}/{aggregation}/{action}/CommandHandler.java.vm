#parse("templates/Cmd.vm")
package $CmdPackagePath;

import org.springframework.stereotype.Component;
import javax.annotation.Resource;

#comment("${CmdDescription}-指令处理器")
@Component
public class ${CmdName}Handler {

    @Resource
    private ${AggregationName}Repository repository;

    public void handle(${CmdName} cmd){

    }
}
